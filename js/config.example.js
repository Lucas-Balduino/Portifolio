// Copie este arquivo para config.js e preencha com suas credenciais EmailJS.
// config.js está no .gitignore — nunca commite chaves reais.

window.EMAILJS_CONFIG = {
  PUBLIC_KEY: 'SUA_PUBLIC_KEY',
  SERVICE_ID: 'SEU_SERVICE_ID',
  TEMPLATE_ID: 'SEU_TEMPLATE_ID'
};
